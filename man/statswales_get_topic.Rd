% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statswales_get_topic.R
\name{statswales_get_topic}
\alias{statswales_get_topic}
\title{Get the contents of a topic}
\usage{
statswales_get_topic(
  topic_id,
  lang = "en-gb",
  page_number = 1,
  page_size = 100,
  sort_by = NULL
)
}
\arguments{
\item{topic_id}{A numeric topic ID. Use \code{\link[=statswales_list_topics]{statswales_list_topics()}} to find IDs.}

\item{lang}{Language for returned text. One of \code{"en-gb"} (default),
\code{"en"}, \code{"cy-gb"}, or \code{"cy"}.}

\item{page_number}{Page number to return. Default \code{1}.}

\item{page_size}{Number of results per page. Default \code{100}.}

\item{sort_by}{Optional sort order for datasets under a leaf topic. Either a
string in \code{"column:direction"} format (e.g. \code{"title:asc"}) or a named
character vector such as \code{c(title = "asc", last_updated_at = "desc")}.}
}
\value{
A list with fields \code{selectedTopic}, \code{children}, \code{parents}, and
\code{datasets}. Returns \code{NULL} if the request fails.
}
\description{
Returns sub-topics or datasets tagged directly to a given topic. Use
\code{\link[=statswales_list_topics]{statswales_list_topics()}} to discover topic IDs.
}
\details{
The returned list has four fields:
\describe{
\item{\code{selectedTopic}}{Details of the requested topic.}
\item{\code{children}}{Sub-topics under this topic. Empty for leaf topics.}
\item{\code{parents}}{Ancestor topics from root to the selected topic.}
\item{\code{datasets}}{A list with \code{data} (dataset list items) and \code{count}.
Only populated for leaf topics (those with no \code{children}).}
}
}
\examples{
\dontrun{
topics <- statswales_list_topics()
topic <- statswales_get_topic(topics$id[1])

# Sub-topics (non-leaf)
topic$children

# Datasets at a leaf topic
topic$datasets$data
topic$datasets$count
}

}
