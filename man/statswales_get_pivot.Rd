% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statswales_get_pivot.R
\name{statswales_get_pivot}
\alias{statswales_get_pivot}
\title{Retrieve a pivot table for a dataset}
\usage{
statswales_get_pivot(
  dataset_id,
  x,
  y,
  lang = "en-gb",
  page_number = 1,
  page_size = 100,
  filter = NULL,
  options = list(use_raw_column_names = FALSE, use_reference_values = FALSE,
    data_value_type = "formatted"),
  sort_by = NULL
)
}
\arguments{
\item{dataset_id}{A dataset UUID string. Use \code{\link[=statswales_list_datasets]{statswales_list_datasets()}} to
find dataset IDs.}

\item{x}{Column name for the horizontal axis (column headers).}

\item{y}{Column name for the vertical axis (row labels).}

\item{lang}{Language for returned text. One of \code{"en-gb"} (default), \code{"en"},
\code{"cy-gb"}, or \code{"cy"}.}

\item{page_number}{Page number to return. Default \code{1}.}

\item{page_size}{Rows per page. Default \code{100}.}

\item{filter}{Filter criteria in the same format as \code{\link[=statswales_get_dataset]{statswales_get_dataset()}}.}

\item{options}{Display options in the same format as \code{\link[=statswales_get_dataset]{statswales_get_dataset()}}.}

\item{sort_by}{Optional sort order, in the same format as
\code{\link[=statswales_get_dataset]{statswales_get_dataset()}}.}
}
\value{
A list containing the paginated pivot view returned by the API, or
\code{NULL} if the request fails.
}
\description{
Returns a cross-tabulated view of dataset data. Choose one dimension for
the columns (\code{x}) and one for the rows (\code{y}). The API stores the pivot
configuration as a reusable query.
}
\examples{
\dontrun{
datasets <- statswales_list_datasets()
id <- datasets$id[1]
filters <- statswales_get_filters(id)

pivot <- statswales_get_pivot(
  id,
  x = filters[[1]]$columnName,
  y = filters[[2]]$columnName
)
}

}
