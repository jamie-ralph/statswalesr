% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statswales_create_query.R
\name{statswales_create_query}
\alias{statswales_create_query}
\title{Create a stored query and return its filter ID}
\usage{
statswales_create_query(
  dataset_id,
  filter = NULL,
  options = list(use_raw_column_names = FALSE, use_reference_values = FALSE,
    data_value_type = "formatted")
)
}
\arguments{
\item{dataset_id}{A dataset UUID string. Use \code{\link[=statswales_list_datasets]{statswales_list_datasets()}} to
find dataset IDs.}

\item{filter}{Filter criteria in the same format as \code{\link[=statswales_get_dataset]{statswales_get_dataset()}}.}

\item{options}{Display options in the same format as \code{\link[=statswales_get_dataset]{statswales_get_dataset()}}.}
}
\value{
A character string containing the 12-character filter ID, or \code{NULL}
if the request fails.
}
\description{
Submits a set of filters and display options to the API and returns the
resulting filter ID. The same inputs always produce the same ID, so this
is useful for building shareable, reproducible data requests.
}
\details{
Pass the returned \code{filter_id} to \code{\link[=statswales_get_query]{statswales_get_query()}} to inspect the
query configuration, or use it directly in \code{\link[=statswales_get_dataset]{statswales_get_dataset()}} /
\code{\link[=statswales_download_dataset]{statswales_download_dataset()}} workflows.
}
\examples{
\dontrun{
datasets <- statswales_list_datasets()
id <- datasets$id[1]

fid <- statswales_create_query(id)
statswales_get_query(id, fid)
}

}
